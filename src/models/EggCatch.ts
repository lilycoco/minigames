export interface HiyokoConfig {
  left: number
  direction: number
  active: boolean
}

export interface EggConfig {
  top: number
  left: number
  dropped: boolean
  catched: boolean
  color: number
}
